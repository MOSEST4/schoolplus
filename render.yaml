services:
  - type: web
    name: schoolplus
    runtime: node
    rootDir: server
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: PORT
        value: 3000
    autoDeploy: true
